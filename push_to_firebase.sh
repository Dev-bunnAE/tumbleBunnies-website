#!/bin/bash

# This script pushes the current local branch to the remote branch named 'Firebase'
# on the 'origin' remote.

# The git push command with origin and the remote branch name
# pushes the local branch to the specified remote branch.
git push origin HEAD:Firebase